[build]
  command = "npm config set registry https://registry.npmjs.org/ && npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10.8.2"
  MISE_DISABLE = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Avoid stale SW/manifest after deploy
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"

[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json; charset=utf-8"
    Cache-Control = "no-cache"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
