import React from "react";
import ReactDOM from "react-dom/client";
import UploadApp from "./UploadApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UploadApp />
  </React.StrictMode>
);
